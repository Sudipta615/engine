[package]
name = "config"
version = "3.30.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/Sudipta615/engine"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
